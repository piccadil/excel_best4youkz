# Apache: serve directory index.html and protect config/logs
DirectoryIndex index.html
Options -Indexes

<Files "config.php">
    Require all denied
</Files>

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^api/logs/ - [F,L]
</IfModule>
